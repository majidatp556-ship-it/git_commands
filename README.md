# git_commands
## sub_heading
- git init (to create a new repositery)
- git status(to check changes)
- git add file name
- git add .
- git commit -m "msg" 
- git push -u origin main
- git push  
- git checkout -b branch_name
- git checkout branch_name